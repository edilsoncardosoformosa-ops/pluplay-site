let voiceEnabled = false, currentQuestions = [], currentIndex = 0, score = 0; // Inicialização do quiz document.addEventListener("DOMContentLoaded", () => { // Gera ou recupera ID do usuário document.getElementById('userIdBox').value = localStorage.getItem('userId') || crypto.randomUUID(); document.getElementById('protectedContent').style.display = 'block'; // Botão copiar ID document.getElementById('copyIdBtn').addEventListener('click', () => { const text = document.getElementById('userIdBox').value; navigator.clipboard.writeText(text).then(() => alert('ID copiado!')); }); // Botão senha manual document.getElementById('confirmPassBtn').addEventListener('click', async () => { const pass = document.getElementById('manualPassInput').value.trim(); if (!pass) { alert('Senha não informada'); return; } const res = await window.tryManualPass(pass); if (res.success) { alert('Acesso liberado via senha!'); renderUserAllowedNote(); } else alert(res.msg); }); // Toggle voz document.getElementById('toggleVoice').addEventListener('click', () => { voiceEnabled = !voiceEnabled; document.getElementById('toggleVoice').textContent = voiceEnabled ? "🔇 Desativar Ler" : "🔊 Ativar Ler"; }); // Inicializa notas de usuário renderUserAllowedNote(); }); // Garantir que quizData seja um objeto com tópicos if (!window.quizData) window.quizData = {}; if (Array.isArray(window.quizData)) { window.quizData = { "Lport": window.quizData }; } // Renderiza nota de usuário function renderUserAllowedNote() { const userId = localStorage.getItem('userId'); const noteDiv = document.getElementById('userAllowedNote'); if (window.allowedUsers.includes(userId)) { noteDiv.innerHTML = <strong style="color:green">Acesso liberado!</strong>; } else noteDiv.textContent = "Demo: acesso limitado a 5 questões por tópico."; } // Iniciar quiz function startQuiz(topic) { let all = window.quizData[topic] || []; const allowed = window.allowedUsers.includes(localStorage.getItem('userId')); if (!allowed) all = all.slice(0, 5); // demo limitado if (all.length === 0) { alert("Sem questões neste tópico."); return; } const num = parseInt(document.getElementById("numQuestions").value) || 10; currentQuestions = shuffle(all).slice(0, num); currentIndex = 0; score = 0; document.getElementById('protectedContent').style.display = 'none'; document.getElementById('quiz').style.display = 'block'; showQuestion(); } // Exibir questão atual function showQuestion() { if (currentIndex >= currentQuestions.length) { endQuiz(); return; } const q = currentQuestions[currentIndex]; document.getElementById('question').textContent = q.q; document.getElementById('comment').textContent = ''; document.getElementById('counter').textContent = Questão ${currentIndex + 1} de ${currentQuestions.length}; if (voiceEnabled) { let u = new SpeechSynthesisUtterance(q.q); u.lang = 'pt-BR'; u.rate = 1.5; window.speechSynthesis.cancel(); window.speechSynthesis.speak(u); } } // Responder questão function answer(choice) { const q = currentQuestions[currentIndex]; const txt = (choice === q.a ? "✓ Correto! " : "✗ Errado! ") + (q.c || ''); document.getElementById('comment').textContent = txt; if (choice === q.a) score++; if (voiceEnabled) { let u = new SpeechSynthesisUtterance(txt); u.lang = 'pt-BR'; u.rate = 1.2; window.speechSynthesis.speak(u); } } // Próxima questão function nextQuestion() { currentIndex++; showQuestion(); } // Voltar ao menu function backToMenu() { document.getElementById('quiz').style.display = 'none'; document.getElementById('result').style.display = 'none'; document.getElementById('protectedContent').style.display = 'block'; } // Finalizar quiz function endQuiz() { document.getElementById('quiz').style.display = 'none'; document.getElementById('result').style.display = 'block'; document.getElementById('score').textContent = Você acertou ${score} de ${currentQuestions.length} questões.; } // Reiniciar quiz function restartQuiz() { document.getElementById('result').style.display = 'none'; document.getElementById('protectedContent').style.display = 'block'; } // Embaralhar array function shuffle(a) { return a.slice().sort(() => Math.random() - 0.5); } o save.php que usa para ligar maste.html aos curso1.html e curso2.html <?php $data = file_get_contents("php://input"); if(!$data){ echo "Nenhum dado recebido"; exit; } $file = "controle.js"; $versionFile = "version.txt"; $js = "var quizData = " . $data . ";"; if(file_put_contents($file, $js)){ // gera versão nova (timestamp) $version = time(); file_put_contents($versionFile, $version); echo "Banco salvo em controle.js (versão $version)"; }else{ echo "Erro ao salvar"; } ?>